Strategy Pattern
---------------------------

Strategy pattern can be confused with Factory pattern.

Strategy is used to define different behavior for one task, in this 
example we use a calculator, the math operations are strategies to
use when the result is retrieved.
